version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
#    environment:
#      MONGO_INITDB_ROOT_USERNAME: user2
#      MONGO_INITDB_ROOT_PASSWORD: password
    ports:
    - 27017:27017
